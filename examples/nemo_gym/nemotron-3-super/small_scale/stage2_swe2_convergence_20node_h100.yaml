defaults: "../stage2_swe2.yaml"

policy:
  generation:
    colocated:
      resources:
        num_nodes: 4
    vllm_kwargs:
      max_num_batched_tokens: 8480

cluster:
  num_nodes: 20   # 16 train (TP8*CP16*PP1=128 GPUs, DP1) + 4 gen (TP8). No judges -> SBATCH=20.
